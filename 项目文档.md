# 时间序列自编码器项目文档

## 📋 项目概述

这是一个基于LSTM的多变量时间序列自编码器(Autoencoder)项目，实现了用于时间序列预测和重构的深度学习模型。该项目实现了论文 [*A Dual-Stage Attention-Based Recurrent Neural Network for Time Series Prediction*](https://arxiv.org/abs/1704.02971) 中描述的双阶段注意力机制。

**GitHub仓库**: [JulesBelveze/time-series-autoencoder](https://github.com/JulesBelveze/time-series-autoencoder)

### 核心特性

- ✅ 支持多变量时间序列处理
- ✅ 双阶段注意力机制（输入注意力 + 时间注意力）
- ✅ 去噪自编码器(Denoising Autoencoder)
- ✅ 稀疏自编码器(Sparse Autoencoder)
- ✅ 支持预测(Forecasting)和重构(Reconstruction)两种任务

---

## 🏗️ 项目架构

### 目录结构

```
time-series-autoencoder/
├── tsa/                          # 核心代码包
│   ├── __init__.py              # 模块初始化
│   ├── model.py                 # 模型定义（编码器和解码器）
│   ├── dataset.py               # 数据集处理
│   ├── train.py                 # 训练逻辑
│   ├── eval.py                  # 评估逻辑
│   └── utils.py                 # 工具函数
├── examples/                    # 使用示例
│   ├── forecasting/            # 预测任务示例
│   │   ├── run_forecasting.py
│   │   └── config.yaml
│   └── reconstruction/         # 重构任务示例
│       ├── run_reconstruction.py
│       └── config.yaml
├── autoenc_architecture.png    # 模型架构图
├── requirements.txt            # 依赖清单
├── pyproject.toml              # 项目配置
└── README.md                   # 项目说明
```

### 模型架构图

![Autoencoder架构](autoenc_architecture.png)

模型由编码器(Encoder)和解码器(Decoder)组成，支持两种注意力机制：
- **输入注意力**：在编码阶段对输入特征进行加权
- **时间注意力**：在解码阶段对编码器的隐藏状态进行加权

---

## 🔧 技术栈

### 核心依赖

| 依赖库 | 版本 | 用途 |
|--------|------|------|
| torch | ≥2.2.0 | 深度学习框架 |
| hydra-core | ≥1.2.0 | 配置管理 |
| pandas | ≥2.2.3 | 数据处理 |
| scikit-learn | ≥1.5.2 | 数据预处理和评估 |
| matplotlib | ≥3.3.4 | 可视化 |
| tensorboardX | ≥2.1 | 训练监控 |
| tqdm | ≥4.59.0 | 进度条 |

---

## 📦 安装指南

### 方法一：使用 uv（推荐）

```bash
# 安装 uv
curl -LsSf https://astral.sh/uv/install.sh | sh  # Linux/Mac
# 或
brew install uv  # Mac with Homebrew

# 克隆仓库
git clone https://github.com/JulesBelveze/time-series-autoencoder.git
cd time-series-autoencoder

# 创建虚拟环境并安装依赖
uv venv
uv pip sync pyproject.toml
```

### 方法二：使用 pip

```bash
# 克隆仓库
git clone https://github.com/JulesBelveze/time-series-autoencoder.git
cd time-series-autoencoder

# 安装依赖
pip install -r requirements.txt
```

---

## 📚 核心模块详解

### 1. model.py - 模型定义

#### 编码器 (Encoders)

**Encoder (基础编码器)**
- 使用标准LSTM处理输入序列
- 逐步生成隐藏状态序列

**AttnEncoder (注意力编码器)**
- 实现输入注意力机制
- 根据当前隐藏状态和历史输入，动态计算输入特征的权重
- 支持去噪功能（训练时添加噪声）
- 关键参数：
  - `input_size`: 输入特征维度
  - `hidden_size`: LSTM隐藏层大小
  - `seq_len`: 序列长度
  - `denoising`: 是否启用去噪

#### 解码器 (Decoders)

**Decoder (基础解码器)**
- 使用LSTM处理历史目标值
- 输出预测结果

**AttnDecoder (注意力解码器)**
- 实现时间注意力机制
- 对编码器输出的隐藏状态序列进行加权
- 结合当前解码器状态和加权的编码器输出生成预测

#### AutoEncForecast (完整模型)

组合编码器和解码器，支持以下配置：
- 是否使用输入注意力 (`input_att`)
- 是否使用时间注意力 (`temporal_att`)

### 2. dataset.py - 数据处理

#### TimeSeriesDataset 类

**核心功能**：
- 数据加载和预处理
- 时间序列窗口滑动
- 数据标准化处理
- 支持分类特征编码（One-Hot）

**支持的任务类型**：
- `prediction`: 预测任务（预测未来值）
- `reconstruction`: 重构任务（重构当前序列）

**关键方法**：

```python
# 初始化
TimeSeriesDataset(
    task: Tasks,              # 任务类型
    data_path: str,           # 数据文件路径
    categorical_cols: List,   # 分类特征列名
    index_col: str,           # 索引列
    target_col: str,          # 目标列
    seq_length: int,          # 序列长度
    batch_size: int,          # 批次大小
    prediction_window: int    # 预测窗口
)

# 获取数据加载器
train_loader, test_loader, nb_features = dataset.get_loaders()

# 反向标准化
original_values = dataset.invert_scale(predictions)
```

### 3. train.py - 训练逻辑

**训练流程**：
1. 初始化TensorBoard日志记录器
2. 设置学习率调度器（StepLR）
3. 迭代训练：
   - 前向传播
   - 计算损失（支持L1/L2正则化）
   - 反向传播
   - 梯度裁剪
   - 参数更新
4. 定期评估和保存检查点

**支持的训练特性**：
- 梯度累积
- 梯度裁剪
- L1/L2正则化（稀疏自编码器）
- 训练中评估
- 自动保存检查点

### 4. eval.py - 评估逻辑

**评估功能**：
- 计算测试集损失
- 生成预测结果
- 保存预测图表
- 计算评估指标（MSE、残差）
- 保存注意力权重

**评估指标**：
- MSE (均方误差)
- Residuals (残差均值)
- Loss (总损失)

### 5. utils.py - 工具函数

**load_checkpoint**: 从检查点文件加载模型状态

---

## 🚀 使用指南

### 基本用法

项目使用 [Hydra](https://hydra.cc/docs/intro/) 进行配置管理。可以通过YAML配置文件或命令行参数设置参数。

```bash
python run_forecasting.py
```

### 配置参数说明

#### 数据配置 (data)

```yaml
data:
  batch_size: 16                    # 批次大小
  categorical_cols: []              # 分类特征列名
  index_col: "Date_Time"            # 索引列
  target_col: ["AH"]                # 目标列
  data_path: "../data/file.csv"    # 数据路径
  prediction_window: 1              # 预测窗口
  seq_length: 3                     # 输入序列长度
  task:
    value: prediction               # 任务类型：prediction 或 reconstruction
```

#### 训练配置 (training)

```yaml
training:
  # 模型架构
  hidden_size_encoder: 64           # 编码器隐藏层大小
  hidden_size_decoder: 64           # 解码器隐藏层大小
  output_size: 1                    # 输出维度
  seq_len: 3                        # 序列长度
  
  # 注意力机制
  input_att: True                   # 是否启用输入注意力
  temporal_att: True                # 是否启用时间注意力
  
  # 去噪设置
  denoising: False                  # 是否启用去噪自编码器
  
  # 正则化
  reg1: True                        # L1正则化
  reg2: False                       # L2正则化
  reg_factor1: 1e-4                 # L1系数
  reg_factor2: 1e-4                 # L2系数
  
  # 训练超参数
  lr: 1e-5                          # 学习率
  num_epochs: 100                   # 训练轮数
  gradient_accumulation_steps: 1    # 梯度累积步数
  max_grad_norm: 0.1               # 梯度裁剪阈值
  lrs_step_size: 5000              # 学习率衰减步长
```

#### 通用配置 (general)

```yaml
general:
  do_train: True                    # 是否训练
  do_eval: True                     # 是否评估
  eval_during_training: True        # 训练中评估
  logging_steps: 100                # 日志记录步数
  save_steps: 5000                  # 保存检查点步数
  output_dir: "output"              # 输出目录
  ckpt: "path/to/checkpoint"        # 检查点路径（评估时使用）
```

### 命令行参数覆盖

```bash
# 覆盖配置参数
python run_forecasting.py training.lr=1e-4 training.num_epochs=50

# 使用不同的配置文件
python run_forecasting.py --config-name=custom_config
```

---

## 📊 示例场景

### 1. 预测任务 (Forecasting)

**目标**：根据历史多变量时间序列预测未来某个变量的值

**示例代码**：`examples/forecasting/run_forecasting.py`

**配置要点**：
- `task: prediction`
- `target_col`: 指定预测目标列
- `output_size: 1`: 单变量预测

**数据集**：[AirQuality数据集](https://gist.github.com/JulesBelveze/e9997b9b0b68101029b461baf698bd72)

### 2. 重构任务 (Reconstruction)

**目标**：学习时间序列的表示，重构输入序列

**示例代码**：`examples/reconstruction/run_reconstruction.py`

**配置要点**：
- `task: reconstruction`
- `target_col: []`: 无目标列
- `output_size`: 等于输入特征数

**数据集**：[重构数据集](https://gist.github.com/JulesBelveze/99ecdbea62f81ce647b131e7badbb24a)

---

## 🔍 模型工作原理

### 编码-解码流程

1. **输入准备**：
   - 输入特征序列: `X ∈ ℝ^(batch_size × seq_len × input_size)`
   - 历史目标值: `y_hist ∈ ℝ^(batch_size × seq_len)`

2. **编码阶段**：
   - 如果启用输入注意力：
     - 计算注意力权重 α_t
     - 加权输入: x'_t = α_t ⊙ x_t
     - LSTM编码: h_t = LSTM(x'_t, h_(t-1))
   - 否则：
     - 直接LSTM编码

3. **解码阶段**：
   - 如果启用时间注意力：
     - 计算编码器隐藏状态的注意力权重
     - 生成上下文向量 context
     - 结合context和当前输入预测
   - 否则：
     - 直接使用最后隐藏状态预测

4. **输出**：
   - 预测值: `ŷ ∈ ℝ^(batch_size × output_size)`

### 注意力机制详解

#### 输入注意力 (Input Attention)

- **目的**：动态选择重要的输入特征
- **计算公式**：
  ```
  e_t = attn_layer([h_(t-1); c_(t-1); X])
  α_t = softmax(e_t)
  x'_t = α_t ⊙ x_t
  ```

#### 时间注意力 (Temporal Attention)

- **目的**：聚焦于编码器输出的重要时间步
- **计算公式**：
  ```
  e_t = attn_layer([h_t^d; c_t^d; H^e])
  β_t = softmax(e_t)
  context = Σ(β_t ⊙ H^e)
  ```

---

## 📈 训练监控

### TensorBoard 可视化

训练过程会在 `output_dir` 目录下生成TensorBoard日志：

```bash
tensorboard --logdir=output
```

**可查看指标**：
- 训练损失曲线
- 测试损失曲线
- 学习率变化
- MSE和残差

### 输出文件

训练完成后，会在输出目录生成以下文件：

```
output/
├── checkpoint-*.ckpt          # 模型检查点
├── preds.png                  # 预测结果图
├── predictions.pt             # 预测值张量
├── targets.pt                 # 真实值张量
├── attentions.pt              # 注意力权重
└── eval_results.txt           # 评估结果报告
```

---

## 🎯 使用建议

### 超参数调优

1. **序列长度 (seq_length)**：
   - 根据数据的时间依赖性选择
   - 建议从3-10开始尝试

2. **隐藏层大小**：
   - encoder和decoder可以设置不同大小
   - 数据复杂时增大（64-128）

3. **学习率**：
   - 建议从1e-5开始
   - 使用StepLR逐步衰减

4. **注意力机制**：
   - 数据特征多时启用input_att
   - 时间依赖复杂时启用temporal_att

5. **正则化**：
   - 过拟合时增加L1正则化系数
   - 建议从1e-4开始调整

### 数据准备建议

1. **数据格式**：
   - CSV格式，包含时间索引列
   - 特征列类型要标记清楚（数值/分类）

2. **预处理**：
   - 数值特征会自动标准化
   - 分类特征需要在配置中指定

3. **训练集划分**：
   - 默认按时间顺序8:2划分
   - 不进行随机打乱（保持时间序列性质）

---

## 🐛 常见问题

### Q1: CUDA out of memory 错误？
**A**: 减小 `batch_size` 或 `seq_length`，或减小隐藏层大小

### Q2: 损失不下降？
**A**: 
- 检查学习率是否过小或过大
- 检查数据是否正确标准化
- 尝试减小正则化系数

### Q3: 如何评估模型性能？
**A**: 查看 `eval_results.txt` 中的MSE和残差，以及 `preds.png` 中的预测曲线

### Q4: 如何加载已训练的模型？
**A**: 
```python
from tsa.utils import load_checkpoint
model, optimizer, loss, epoch = load_checkpoint(
    "output/checkpoint-5000.ckpt", 
    model, 
    optimizer, 
    device
)
```

---

## 📄 许可证

本项目使用 LICENSE 文件中指定的许可证。

---

## 🙏 致谢

- 论文: [A Dual-Stage Attention-Based Recurrent Neural Network for Time Series Prediction](https://arxiv.org/abs/1704.02971)
- 灵感来源: [Seanny123's da-rnn repository](https://github.com/Seanny123/da-rnn)

---

## 📧 联系方式

如有问题或建议，请访问 [GitHub仓库](https://github.com/JulesBelveze/time-series-autoencoder) 提交Issue。

---

**文档版本**: v1.0  
**最后更新**: 2024年10月
